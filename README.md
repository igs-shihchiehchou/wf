# 🎵 音效處理工具

一個基於瀏覽器的音效處理靜態網站，提供音訊載入、編輯與匯出功能。無需安裝，無需伺服器，純前端運行。

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Status](https://img.shields.io/badge/status-stable-green.svg)

## ✨ 功能特色

### 🎛️ 音訊編輯
- **裁切** - 雙手柄滑桿精確選擇音訊區間
- **音量調整** - 0-200% 靈活控制音量
- **淡入效果** - 可自訂淡入時長（0-10秒）
- **淡出效果** - 可自訂淡出時長（0-10秒）
- **速度調整** - 0.5x-2.0x 播放速率調整

### 🔄 工作流程
- **串接式編輯** - 處理結果自動產生新卡片，支援多層編輯
- **即時預覽** - 波形視覺化顯示，隨時播放試聽
- **檔案匯出** - 下載處理後的音訊為 WAV 格式

### 💻 技術特點
- **零建置** - 純靜態網頁，直接開啟即用
- **CDN 載入** - 無需 npm，所有函式庫透過 CDN 引入
- **離線處理** - 所有處理在瀏覽器本地完成，資料不上傳
- **響應式設計** - 支援桌面、平板、手機

## 🚀 快速開始

### 線上使用
直接訪問 GitHub Pages 部署版本：
```
https://[username].github.io/audio-webtool/
```

### 本地開發

1. **克隆專案**
```bash
git clone https://github.com/[username]/audio-webtool.git
cd audio-webtool
```

2. **啟動本地伺服器**

使用 Python（推薦）：
```bash
python -m http.server 8000
```

或使用其他方式：
```bash
# Node.js
npx http-server -p 8000

# PHP
php -S localhost:8000

# VS Code Live Server
# 安裝 Live Server 擴充功能，右鍵點擊 index.html → Open with Live Server
```

3. **在瀏覽器開啟**
```
http://localhost:8000
```

## 📖 使用說明

### 1️⃣ 上傳音訊
- 點擊上傳區域選擇檔案
- 或直接拖拉音訊檔案到上傳區域
- 支援格式：MP3, WAV, OGG, M4A

### 2️⃣ 編輯音訊
每張音訊卡片提供以下控制：

**播放控制**
- 點擊 ▶ 播放/暫停
- 拖動波形圖跳轉播放位置

**裁切**
- 勾選「裁切」核取框
- 拖動雙手柄滑桿選擇範圍
- 查看底部時間顯示確認選取區間

**音量**
- 拖動音量滑桿（0-200%）
- 100% 為原始音量

**淡入淡出**
- 勾選「淡入」或「淡出」
- 設定效果持續時間（秒）

**速度**
- 拖動速度滑桿（0.5x-2.0x）
- 1.0x 為原始速度

### 3️⃣ 執行處理
- 點擊「執行處理 →」按鈕
- 系統會處理音訊並產生新卡片
- 新卡片自動繼承處理結果
- 可在新卡片上繼續編輯

### 4️⃣ 下載結果
- 點擊卡片右上角「下載」按鈕
- 音訊將以 WAV 格式下載

### 5️⃣ 管理卡片
- 點擊右上角 **×** 刪除卡片
- 使用 `Ctrl/Cmd + Shift + C` 清除所有卡片

## 🎨 設計系統

### 色彩主題
專案採用深色主題設計，減少長時間使用的眼睛疲勞：
- **主色調**：暖色系米黃 `hsl(56 38% 57%)`
- **次要色**：柔和紫藍 `hsl(242 68% 80%)`
- **背景色**：深色調 `hsl(56-58 / 1-8%)`

完整設計規範請參考 [doc/plan.md](doc/plan.md)

## 🛠️ 技術棧

### 前端技術
- **HTML5** - 語義化標籤
- **CSS3** - CSS 變數、Flexbox、響應式設計
- **Vanilla JavaScript** - ES6+ 語法，模組化架構

### 音訊處理
- **Web Audio API** - 瀏覽器原生音訊處理
- **Tone.js** (CDN) - 高階音訊處理庫
- **WaveSurfer.js v7** (CDN) - 音訊波形視覺化

### 樣式框架
- **Tailwind CSS** (CDN) - 快速樣式開發
- **自訂 CSS** - 完整設計系統

## 📁 專案結構

```
audio_webtool/
├── index.html              # 主頁面
├── css/
│   └── theme.css          # 設計系統與樣式
├── js/
│   ├── app.js             # 主應用程式邏輯
│   ├── audioCard.js       # 音訊卡片組件
│   ├── audioProcessor.js  # 音訊處理引擎
│   └── utils.js           # 工具函數
├── assets/
│   └── icons/             # 圖示資源
├── doc/
│   ├── plan.md            # 開發計劃與設計規範
│   ├── task.md            # 任務追蹤清單
│   └── theme.md           # 色彩主題定義
├── .gitignore
└── README.md
```

## 🔧 開發

### 架構說明

**模組化設計**
- `utils.js` - 通用工具函數（時間格式化、Toast 提示等）
- `audioProcessor.js` - 音訊處理核心（裁切、音量、淡入淡出等）
- `audioCard.js` - 音訊卡片 UI 組件
- `app.js` - 應用程式主控制器

**資料流**
```
檔案上傳 → AudioBuffer → AudioCard
                           ↓
                    使用者編輯參數
                           ↓
                    AudioProcessor 處理
                           ↓
                  新 AudioBuffer → 新 AudioCard
```

### 核心類別

**AudioProcessor**
```javascript
- loadAudioFromFile(file)     // 載入音訊檔案
- cropAudio(buffer, start, end) // 裁切
- adjustVolume(buffer, gain)   // 調整音量
- applyFadeIn(buffer, duration) // 淡入
- applyFadeOut(buffer, duration) // 淡出
- changePlaybackRate(buffer, rate) // 速度
- processAudio(buffer, settings) // 整合處理
```

**AudioCard**
```javascript
- constructor(audioBuffer, filename, parentCard)
- initialize()              // 初始化 WaveSurfer
- togglePlay()             // 播放/暫停
- processAudio()           // 執行處理
- downloadAudio()          // 下載
- delete()                 // 刪除卡片
```

**CardsManager**
```javascript
- addCard(card)            // 新增卡片
- removeCard(cardId)       // 移除卡片
- clearAll()               // 清除所有
```

## 🧪 測試

### 功能測試
- ✅ 檔案上傳（點擊、拖拉）
- ✅ 音訊播放控制
- ✅ 裁切功能
- ✅ 音量調整
- ✅ 淡入淡出
- ✅ 速度調整
- ✅ 多層處理串接
- ✅ 檔案下載

### 瀏覽器相容性
- ✅ Chrome (推薦)
- ✅ Firefox
- ✅ Safari
- ✅ Edge

**最低需求：** 支援 Web Audio API 的現代瀏覽器

## 📝 已知限制

### 技術限制
- 速度調整為簡易實作，會改變音高
- 大檔案（> 50MB）可能影響效能
- 某些老舊瀏覽器不支援

### 功能限制
- 僅支援單聲道/立體聲音訊
- 匯出格式固定為 WAV
- 無法儲存編輯專案

## 🚧 未來擴充

### 計劃中的功能
- [ ] EQ 等化器
- [ ] Reverb 迴音效果
- [ ] 多軌混音
- [ ] 專案儲存/載入
- [ ] 更多匯出格式

### 改善項目
- [ ] 使用 Web Workers 優化大檔案處理
- [ ] 實作 pitch-preserving 速度調整
- [ ] 增加批次處理功能

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

1. Fork 本專案
2. 建立功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 開啟 Pull Request

### Commit 規範
```
feat: 新功能
fix: 錯誤修復
docs: 文檔更新
style: 樣式調整
refactor: 重構
perf: 性能優化
test: 測試相關
```

## 📄 授權

本專案採用 MIT 授權條款 - 詳見 [LICENSE](LICENSE) 檔案

## 👨‍💻 作者

Generated with [Claude Code](https://claude.com/claude-code)

## 🙏 致謝

- [Tone.js](https://tonejs.github.io/) - 音訊處理函式庫
- [WaveSurfer.js](https://wavesurfer-js.org/) - 波形視覺化
- [Tailwind CSS](https://tailwindcss.com/) - CSS 框架

## 📞 聯絡方式

有問題或建議？歡迎開 Issue 討論！

---

⭐ 如果這個專案對你有幫助，請給個星星支持！
